import React from 'react';

// ImageSizeValidator component - Disabled for POS system
// No longer validates image constraints per user request
const ImageSizeValidator = () => {
  return null;
};

export default ImageSizeValidator;